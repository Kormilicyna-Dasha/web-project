/*
document.addEventListener('DOMContentLoaded', () => {
    const links = document.querySelectorAll('.nav-link'); // Ссылки навигации
    const currentUrl = window.location.pathname; // Получение текущего пути (к примеру, /about.html)

    links.forEach(link => {
        // Если href ссылки совпадает с текущим путем
        if (link.getAttribute('href') === currentUrl) {
            link.classList.add('underlined-link'); // Добавляем класс для подсветки
            link.classList.add('.nav-link:after');
        }

    });
});

document.addEventListener('DOMContentLoaded', () => {
    const links = document.querySelectorAll('.down-links'); // Ссылки навигации
    const currentUrl = window.location.pathname; // Получение текущего пути (к примеру, /about.html)

    links.forEach(link => {
        // Если href ссылки совпадает с текущим путем
        if (link.getAttribute('href') === currentUrl) {
            link.classList.add('changecolor-link'); // Добавляем класс для подсветки
            
        }

    });
});
*/